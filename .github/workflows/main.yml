name: Deploy Back-end

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 16.13
        
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: alonypau@gmail.com
        password: dckr_pat_qW7lH1E-1I_0u2twzhykFmjrUQk

    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        push: true
        tags: 6030631621/candidate-test:latest